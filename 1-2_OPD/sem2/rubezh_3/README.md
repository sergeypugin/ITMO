# Рубеж №3: массивы и массивный код

<img src="resources/how_task_is_looking.jpg">

## Вступление

Перед тем, как погрузиться в дебри БЭВМ, сначала расскажу, что из себя представляет третий рубеж:
1. Вам предстоит написать программу для работы с каким-то массивом в памяти
2. Необходимо будет работать с двумя словами (две ячейки, в сумме 32 бита, а не привычные 16), причём изначально их длина будет меньше, а значит потребуется реализовать расширение знака
3. После того, как Вы напишите код, Вы попробуете его скомпилить на вот такой вот платформе (впрочем, тут Вы его и писали всё это время)

**Важно знать**:
1. Как и БЭВМ, платформа попытается скомпилить Ваш код и выдаст ошибки, если таковые есть (очень удобно, если Вас преследуют со школьных лет опечатки по типу `VALL FUNC` или Вы вдруг забыли, что такое адресация и написали, например, `LD #CUR`)
2. Пытаться сделать тест и ожидать результата в нужном месте в памяти - скорее всего пустая трата времени. Лично я так и не понял, когда платформа останавливается, поскольку написав `HLT` в самое начало, она уверенно пошла к своей цели - выполнить код во что бы то ни стало)
3. Если вдруг Ваша программа содержит какую-то неточность - перепись!
4. Т.к. практики будут проверять, то рекомендуется оставлять комментарии, описывающие логику (но писать `CLA; очищаем аккумулятор` не стоит)
5. После рубежа будет назначен доп, на котором Вы должны будете показать ваши исправления кода и пояснить за саму прогу, а практик уже решит, что с Вами делать
6. Ладно, п.3 для развлечения и поддержания Вашего внимания

Для тех, кто уже написал рубеж и готовится к тому, чтобы найти ошибки и поправить свою работу:
<img src="resources/google_table.png">
Выделите две ячейки, что содержат ваше задание и ваш код, поместите содержимое в `A.txt` и воспользуйтесь парсером питоновским:

```PYTHON
with open("A.txt") as file1:  
    text="".join(file1.readlines()).split("\"")  
    task=text[1]  
    code=text[-2]  
    with open("B.txt",'w') as file2:  
        file2.write("".join(f";{i}\n" for i in task.split("\n")))  
        for part in code.split("\\"):  
            file2.write('\n'+part.split('n')[-1])
```

Нужный Вам код с закомменченным заданием ожидает Вас в `B.txt`

А теперь к самому долгожданному.

## Теория к рубежу

### Основные сокращения, знания которых помогают понять, что происходит:
* `PTR` - PoinTeR - указатель на массив или что-то ещё
* `CURH`, `CURL` - CURrent High, CURrent Low - старшее и младшее слово какой-то временной переменной

### Адресация 

Значит, существует несколько видов работы с памятью (та самая, что зовётся "виды адресации"), но нам понадобятся только эти (не обязательно знать их название, главное уметь использовать):
1. Прямая загрузка: `LD #4`, `ADD #10` - работа с числом после символа '#'. 
	**Важно**: `ADD #10` прибавит к `AC` не двойку в десятичной, а именно десятку, т.к. БЭВМ по умолчанию работает с десятичными числами
2. Прямая относительная: `ADD X`, `OR Y` - работа с числом, что лежит в ячейке с меткой аргумента.
	**Важно**: работает только, когда до ячейки прыгать не более чем 128 ячеек
3. Косвенная: `LD (PTR)` - работа с ячейкой, что находится по адресу `PTR` (отсюда и сокращение в последующих моих работах - pointer)
4. Адресация со смещением по стеку: `LD &1`, `ADD &2` - работа не только с вершиной, но и с другими элементами стека по порядковому номеру относительно вершины. Вершина - 0-позиция, под ней 1-ая позиция и т.д.
	**Важно**: если вдруг Вы просчитаетесь с номером и возьмёте без спросу адрес возврата при вызове подпрограммы, логика полетит и скорее всего Вы пролетите даже `HLT`. Особенно часты ошибки, когда в подпрограмме есть другая подпрограмма.

***
 ***Замечение***:
 Прямая относительная (`LD X`) и адресация со смещением по стеку(`LD &1`) предназначены для одного и того же - работать с памятью по ячейке, которую уже куда-то сохранили. Вторая куда лучше тем, что в ней нет ограничения по отдалённости ячейки, из-за чего используется в серьёзных программах куда чаще. Однако связи с тем, что стек у нас ездит во все стороны и используется для подпрограмм, а метки переменных остаются всегда, советую избегать эти смещения и пользоваться прямой относительной адресацией.
***
**Очень полезная вещь**:
Ещё не забыли, что значит `LD (PTR)`?) Если забыли, см. выше.
Можно использовать `(PTR)+` и `-(PTR)`, т.е. в 1-м случае после обращения к памяти по адресу `PTR` и взятия нужного аргумента `PTR` будет инкрементироваться (+1),
а во 2-м случае до обращения к памяти - декрементироваться (-1).
Такие возможности ассемблера позволяют осуществлять удобный перебор массивов, что, скорее всего, Вы использовали/будете использовать в лабах.
Ещё один наиудобнейший момент: если у Вас есть какой-то счётчик/индекс `CURID`, то вместо
```
LD CURID
INC
ST CURID
```
Вы имеете право сделать лишь
```
LD (CURID)+
```
, что позволит инкрементировать счётчик лишь одной командой!
Однако будьте готовы к тому, что Вы потеряете предыдущее содержимое `AC`, записав в него счётчик (который ещё не инкрементировался). Тоже очень удобная фича для работы с массивами, да и в принципе с циклами. Уверен, к этой рубежке это Вы освоили)

### Команды ветвления

Что касается сравнений чисел, то важно знать команды ветвлениия:

| Команда | Расшифровка                | Условие перехода | Когда применять                                                           |
| :------ | :------------------------- | :--------------- | :------------------------------------------------------------------------ |
| `BR`    | Branch                     | Без условий      | Безусловный короткий переход в пределах $\pm 128$ слов                    |
| `BEQ`   | Branch if Equal            | `Z == 1`         | При равенстве чисел (после `CMP`) или обнулении счетчика                  |
| `BNE`   | Branch if Not Equal        | `Z == 0`         | При неравенстве чисел или ненулевом результате                            |
| `BMI`   | Branch if Minus            | `N == 1`         | Если результат операции отрицательный (знаковый)                          |
| `BPL`   | Branch if Plus             | `N == 0`         | Если результат операции положительный или равен нулю                      |
| `BLT`   | Branch if Less Than        | `N ⊕ V == 1`     | **Для знаковых чисел**: переход, если "меньше" (защищает от переполнения) |
| `BGE`   | Branch if Greater or Equal | `N ⊕ V == 0`     | **Для знаковых чисел**: переход, если "больше или равно"                  |
| `BLO`   | Branch if Lower            | `C == 0`         | **Для беззнаковых чисел** (например, младших слов): переход, если "ниже"  |
| `BHIS`  | Branch if HIgher or Same   | `C == 1`         | **Для беззнаковых чисел**: переход, если "выше или равно"                 |
| `BVS`   | Branch if Overflow Set     | `V == 1`         | Если произошло знаковое переполнение                                      |
| `BVC`   | Branch if Overflow Clear   | `V == 0`         | Если знакового переполнения не было                                       |

***
***Замечание***:
Когда мы говорим "переход, если >" или что-то типо того, то имеется в виду, что в `AC` было `A`, затем мы вычли `B`, а дальше при помощи `NZVC` мы определяем, что да как. Там есть конечно логика, но ну её, как будто её кто-то будет спрашивать
***
### Тесты

Вам понадобится сделать тесты (по крайней мере после рубежки точно), ибо как Вы будете уверены в том, что программа работает?

**MUST HAVE**: числа 0, 1, -1, 2 (просто проверить ручками, что функция правильно сработает), максимум и минимум в вашей разрядной сетке. 
***
**Важно**: вместо того, чтобы переводить 2 из 10-чной в 16-ричную, можно просто писать `WORD 2`, а не `WORD 0x10` (т.к. по умолчанию в БЭВМ работает с 10-чными числами, уже писал в начале).
***
Остальное - уже на ваше усмотрение. Ну и не забывайте пользоваться калькулятором для вычисления значений функций, он очень удобен, особенно учитывая то, что Вы можете в нём вводить сначала шестнадцатеричные, а затем переключаться на десятичные)

Вычисление $5_{10} \times 10_{16} = 50_{16} = 80_{10}$:

<img src="resources/computing_in_diff_NS.gif" width="300">

С проверкой отрицательных чисел бывает тяжело, так что воспользуйтесь режимом QWORD в калькуляторе (тыкайте до упора, когда дойдёте до него, будет работа с 32-битными числами, а сверху уже появится "DWORD" для работы с 16-битными) и тыкните кнопку "+/-".

<img src="resources/computing_neg_numbers.gif" width="300">

Также для тестов удобно делать `ARR: WORD 10 DUP (0)`, чтобы заполнять оставшийся массив нулями.
**НО!** Использовать следует максимум 4-5 тестовых и обрубать длину-счёткик для `LOOP` на момент тестирования! Иначе состаритесь в ожидании результата (в среднем для каждого вида программ уходит минуты 3-4 лишь на тесты массива длины 4-5)

И также убедительная просьба не забывать писать `ORG 0xABC`, иначе ваши данные будут не по тому адресу, который ожидается программой и был дан по заданию.

Пример теста для массива длины 13 (хотя в проге нужно изменить длину, чтобы не состариться):

``` ASSEMBLY
;TEST DATA:
ORG 0x6df
WORD 10
WORD 1
WORD 0x7FFF;
WORD 0x5FFF;
WORD 8 DUP (0); пусть мусор из тех ячеек массива очистится
```

А теперь перейдём к самим заданиям.

## Что касается рубежа, то...

### Типы заданий рубежки

Поскольку как я уже писал, практики соберут всё в табличку, скинут на неё ссылку и попросят поправить свои ошибки или пояснить за код, произошла маааленькая утечка всех возможных вариаций заданий.

"Бланк заданий" я оставил, но давай те разберёмся, что в нём. Все они делятся на всего лишь **5** типов! **P.S.**: сори, но самому анализировать я это не возьмусь, лучше дам Вам ответ специалиста
Вот те самые типы:
#### Тип 1А: «Халявный» массив функций
*   **Особенность:** Исходные элементы $\le 16$ бит (читаем по 1 слову). Результат функции гарантированно $\le 32767$. **Математику делаем в 16 битах, а знак расширяем до 32 бит только в конце перед записью.**
*   **Задачи:** **2, 4, 7, 8, 9, 22** (Всего: 6 задач).

#### Тип 1B: «Честный» массив функций
*   **Особенность:** Исходные элементы $\le 16$ бит (читаем по 1 слову). Но результат функции $> 32767$ и переполняет 16-битную сетку. **Требуются 32-битные сдвиги (`ASL/ROL`) и сложения (`ADD/ADC`) со старта.**
*   **Задачи:** **11, 14, 23** (Всего: 3 задачи).

#### Тип 2А: Сумма (SUM) в 32 битах
*   **Особенность:** Исходные элементы $> 16$ бит (читаем по 2 слова). Сложение (`ADD` + `ADC`) 32-битных чисел. Результат — одно 32-битное число.
*   **Задачи:** **5, 10, 18 (21 — дубликат)** (Всего: 4 задачи).

#### Тип 2B: Поиск экстремума (MIN / MAX) в 32 битах
*   **Особенность:** Исходные элементы $> 16$ бит (читаем по 2 слова). Знаковое сравнение 32-битных чисел (по старшему, затем по младшему слову). Результат — одно 32-битное число.
*   **Задачи:** **1, 13, 15, 17 (20 — дубликат)** (Всего: 5 задач).

#### Тип 2C: Логика (AND / XOR) в 32 битах
*   **Особенность:** Исходные элементы $> 16$ бит (читаем по 2 слова). Логические операции на уровне 32 бит. Особый ад в виде XOR без команды XOR. Результат — одно 32-битное число.
*   **Задачи:** **3, 6, 12, 16 (19 — дубликат)** (Всего: 5 задач).

---
### Твой индивидуальный список задач для тренировки

Вот 5 конкретных условий задач из файла, представляющие каждый из 5 классов. Решения представлены в папке  `codes` и пояснения к ним будут в следующем блоке `Решения`

#### 1. Тип 1А: «Халявный» массив функций (Задача №2)
```text
;Задание №1. Разработать программу для работы с элементами массива M, в которой:
;1. Массив имеет следующие характеристики:
;- адрес начала массива в памяти БЭВМ - 0x6e6;
;- число измерений исходного массива - 1;
;- количество элементов исходного массива - 13;
;- каждый элемент является знаком числом с разрядностью 12 бит;
;- нумерация элементов начинается с 1;
;- элементы хранятся в массиве по границам слов, нет необходимости в плотной упаковке;
;2. Для элементов массива необходимо вычислить 32-х битное значение функции:
;- формула функции F(Mi) = 13 * Mi + 1425;
;- 32-битный результат необходимо поместить в другой массив по адресу 0x400
;- Результатом является массив 32-х разрядных чисел равным количеству элементов исходного массива.
;Примечание: все числа представлены в десятичной системе счисления, если явно не указано иное.
```
*(Почему халявный: макс. $M_i = 2047$. $13 \cdot 2047 + 1425 = 28036 \le 32767$. Всё влезает в обычные 16 бит!)*
#### 2. Тип 1B: «Честный» массив функций (Задача №11)
```text
;Задание №1. Разработать программу для работы с элементами массива M, в которой:
;1. Массив имеет следующие характеристики:
;- адрес начала массива в памяти БЭВМ - 0x6df;
;- число измерений исходного массива - 1;
;- количество элементов исходного массива - 14;
;- каждый элемент является знаковым числом с разрядностью 14 бит;
;- нумерация элементов начинается с 1;
;- элементы хранятся в массиве по границам слов, нет необходимости в плотной упаковке;
;2. Для элементов массива необходимо вычислить 32-х битное значение функции:
;- формула функции F(Mi) = 14 * Mi + 4689;
;- 32-битный результат необходимо поместить в другой массив по адресу 0x400
;- Результатом является массив 32-х разрядных чисел равным количеству элементов исходного массива.
;Примечание: все числа представлены в десятичной системе счисления, если явно не указано иное.
```
*(Почему сложный: макс. $M_i = 8191$. $14 \cdot 8191 + 4689 = 119363 > 32767$. Здесь обязательно считать через 32-битные `ASL/ROL` и `ADD/ADC` со старта).*

#### 3. Тип 2А: Сумма в 32 битах (Задача №10)
```text
;Задание №1. Разработать программу для работы с элементами массива M, в которой:
;1. Массив имеет следующие характеристики:
;- адрес начала массива в памяти БЭВМ - 0x6e1;
;- число измерений исходного массива - 1;
;- количество элементов исходного массива - 13;
;- каждый элемент является знаковым числом с разрядностью 22 бит;
;- нумерация элементов начинается с 3;
;- элементы хранятся в массиве по границам слов, нет необходимости в плотной упаковке;
;2. Для элементов массива необходимо вычислить одно значение по правилам:
;- агрегировать необходимо только для элементы массива с кратными 3-м i-индексами;
;- из выбранных элементов необходимо вычислить cумму значений и записать результат в память по адресу 0x400.
;- Результатом является одно 32-х разрядное число!
;Примечание: все числа представлены в десятичной системе счисления, если явно не указано иное.
```

#### 4. Тип 2B: MIN/MAX в 32 битах (Задача №15)
```text
;Задание №1. Разработать программу для работы с элементами массива M, в которой:
;1. Массив имеет следующие характеристики:
;- адрес начала массива в памяти БЭВМ - 0x6ce;
;- число измерений исходного массива - 1;
;- количество элементов исходного массива - 18;
;- каждый элемент является знаковым числом с разрядностью 25 бит;
;- нумерация элементов начинается с 2;
;- элементы хранятся в массиве по границам слов, нет необходимости в плотной упаковке;
;2. Для элементов массива необходимо вычислить одно значение по правилам:
;- агрегировать необходимо только для элементы массива с кратными 2-м i-индексами;
;- из выбранных элементов необходимо вычислить минимальное значение и записать результат в память по адресу 0x400.
;- Результатом является одно 32-х разрядное число!
;Примечание: все числа представлены в десятичной системе счисления, если явно не указано иное.
```

#### 5. Тип 2C: Логика / XOR в 32 битах (Задача №16)
```text
;Задание №1. Разработать программу для работы с элементами массива M, в которой:
;1. Массив имеет следующие характеристики:
;- адрес начала массива в памяти БЭВМ - 0x6e0;
;- число измерений исходного массива - 1;
;- количество элементов исходного массива - 16;
;- каждый элемент является знаковым числом с разрядностью 21 бит;
;- нумерация элементов начинается с 0;
;- элементы хранятся в массиве по границам слов, нет необходимости в плотной упаковке;
;2. Для элементов массива необходимо вычислить одно значение по правилам:
;- агрегировать необходимо только для элементы массива с четными i-индексами;
;- из выбранных элементов необходимо вычислить исключающее 'ИЛИ' значений и записать результат в память по адресу 0x400.
;- Результатом является одно 32-х разрядное число!
;Примечание: все числа представлены в десятичной системе счисления, если явно не указано иное.
```

## Решения

Решения всех представленных выше заданий Вы сможете найти в папке `codes`, как я уже говорил ранее. Также в папке `my_rubezh`, как Вы могли догадаться, я выложил свой рубеж и отчёт по исправлениям.

***
### ***Замечание***:

Эта информация довольно-таки важная и не самая простая, возможно стоит сначала прорешать/посмотреть решения всех типов заданий выше, а только потом уже вернуться сюда и дочитать этот пункт.

А теперь к тому, что я хотел уточнить:

Во-первых, как оказалось, у БЭВМ есть исходники: [tune-it/bcomp](https://github.com/tune-it/bcomp)
Просить Вас ознакомиться с ними я конечно не буду (если интересно, никто не мешает эту огромную репу скормить сверхразуму), но отмечу одно безмерно важное ***НО***, с которым я столкнулся. 

Чтобы структурировать свой рассказ легче, давайте просто будем пытаться ответить на вопрос: откуда начинается программа? В смысле с какой ячейки кладётся весь код, что мы натыкали, а также какая команда будет выполняться первой (или по умолчанию первая команда - первая по счёту, т.е. нельзя ,например, положить переменные в начало проги, а саму прогу написать потом)?

И вопрос этот очень не простой. Для начала напомню, что существует такая команда как `ORG 0xABC` - перемещает Вас в `0xABC`. Многие из Вас наверняка даже не задумывались, что будет, если перед началом проги не писать `ORG 0xABC`, который отправит Ваш код в нужное место. При помощи сверхразума и исходников, я нашёл, что в файлах [uno](https://github.com/tune-it/bcomp/blob/v2-ng-master/bcomp-assembler/src/main/java/ru/ifmo/cs/bcomp/assembler/AsmNg.java), [dos](https://github.com/tune-it/bcomp/blob/v2-ng-master/bcomp-ng/src/main/java/ru/ifmo/cs/bcomp/ProgramBinary.java) выстраивается такая логика:
* По поводу `ORG 0xABC` и где будет располагаться программа:
	Если в программе есть `ORG 0xABC`:
		Программа будет перенесена в часть памяти, что начинается с адреса`ABC`
	Иначе:
		Программа будет перенесена в часть памяти, что начинается  с адреса `0x10` (как раз освобождается место под все вектора прерываний)

* По поводу того, откуда будет программа начинаться:
	Если в программе есть метка `START`, `НАЧАЛО` или `ЕДУ`:
		Программа будет запускаться с этой метки
	Иначе:
		Программа будет запускаться с той команды, что стоит самой первой

Это довольно важно знать, чтобы первым делом проверить этот пункт, если программа сломалась. Понимаю, нелегко, но для себя я нашёл самый простой вариант: просто ставьте метку `START` перед тем местом, откуда хотите, чтобы начиналась программа, а про `ORG 0xABC` - обязательно используйте его для того, чтобы размещать тестовые данные (при просмотре заданий и их решений Вы поймёте о чём я, т.к. программа, которую Вы будете писать, предназначена для работы с массивом, что находится по конкретному адресу). В остальных местах программы `ORG 0xABC` советую не использовать.
***

На этом у меня всё, удачи на рубежке!